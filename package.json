{
  "name": "orggraph",
  "version": "1.0.0",
  "description": "OrgGraph Visualization — dependency-free single-file app (index.html)",
  "type": "module",
  "scripts": {
    "build": "node build.js",
    "verify": "node build.js && node verify.js",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "transform": "node helpers/transform.js"
  },
  "devDependencies": {
    "@babel/generator": "^7.28.5",
    "@babel/parser": "^7.28.5",
    "@babel/traverse": "^7.28.5",
    "@babel/types": "^7.28.5",
    "@vitest/coverage-v8": "^3.2.4",
    "fake-indexeddb": "^6.0.1",
    "jsdom": "^26.1.0",
    "vitest": "^3.2.4"
  }
}

# Local data & config (examples are tracked)
data/*
env*.json
!env.example.json
attributes

# Dependencies & build output
node_modules
dist

# Test & coverage artifacts
coverage
coverage-report
playwright-report
monocart-report
test-results

# Scratch
temp
tmp
trace

# Local tooling
.vscode
.claude

# Local data pipeline & tooling (kept local, not shipped)
helpers
PRD-Rebuild-Screenshot.png
